<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Default" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>

        <item name="windowActionModeOverlay">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="actionModeBackground">@color/accent</item>
        <item name="actionModeCloseDrawable">@drawable/ic_close_white_24dp</item>

        <item name="android:actionBarStyle">@style/Theme.Default.ActionBarStyle</item>
    </style>

    <style name="Theme.Default.Activity" parent="Theme.Default">


    </style>

    <style name="Theme.Default.Dialog" parent="Theme.AppCompat.Light.Dialog">

        <item name="colorAccent">@color/primaryDark</item>
    </style>

    <style name="Theme.Default.ActionBarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">

    </style>

</resources>